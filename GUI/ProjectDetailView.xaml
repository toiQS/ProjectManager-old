<Window x:Class="GUI.ProjectDetailView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GUI"
        mc:Ignorable="d"
        Title="Project Detail" Height="773" Width="600">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Tên dự án -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Width="100" Text="Tên dự án:" VerticalAlignment="Center"/>
            <TextBox Width="400" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Ngày bắt đầu và kết thúc -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Width="100" Text="Ngày bắt đầu:" VerticalAlignment="Center"/>
            <DatePicker Width="150" VerticalAlignment="Center"/>
            <TextBlock Width="100" Margin="20,0,0,0" Text="Ngày kết thúc:" VerticalAlignment="Center"/>
            <DatePicker Width="150" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Số thành viên -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Width="100" Text="Số thành viên:" VerticalAlignment="Center"/>
            <TextBox Width="50" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Miêu tả dự án -->
        <StackPanel Grid.Row="3" Margin="0,20,0,10">
            <TextBlock Text="Miêu tả dự án:" VerticalAlignment="Top"/>
            <TextBox TextWrapping="Wrap" AcceptsReturn="True" Height="150" VerticalScrollBarVisibility="Auto"/>
        </StackPanel>

        <!-- Các nút chức năng -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" VerticalAlignment="Bottom">
            <Button Content="Trở lại" Width="100" Margin="0,20,10,10" Click="Back_Click"/>
            <Button Content="Quản lý thành viên" Width="100" Margin="0,20,10,10" Click="ManageMembers_Click"/>
            <Button Content="Chỉnh sửa dự án" Width="100" Margin="0,20,10,10" Click="EditProject_Click"/>
            <Button Content="Xóa" Margin="0,20,10,10" Width="83" Click="DeleteProject_Click"/>
            <Button Content="Quản lý tiến độ" Margin="0,20,10,10" Width="112" Click="ManageProgress_Click"/>
        </StackPanel>

        <!-- Additional controls (if needed) -->
        <ComboBox Grid.Row="3" HorizontalAlignment="Left" VerticalAlignment="Top" Width="284" Margin="158,211,0,0"/>
        <Label Grid.Row="3" Content="Tình trạng dự án" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,214,0,0"/>
        <Label Grid.Row="3" Content="Người khởi tạo dự án" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,245,0,0"/>
        <TextBox Grid.Row="3" Margin="158,249,0,0" VerticalAlignment="Top" Height="18" Width="216"/>
        <DatePicker Grid.Row="3" HorizontalAlignment="Left" VerticalAlignment="Top" Width="182" Height="25" Margin="158,287,0,0"/>
        <Label Grid.Row="3" Content="Ngày khởi tạo" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,287,0,0" Height="25" Width="107"/>
        <Label Grid.Row="3" Content="Thông tin dự án" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,326,0,0" Height="25" Width="107"/>
        <TextBox Grid.Row="3" TextWrapping="Wrap" VerticalAlignment="Top" Height="216" Width="560" Margin="0,356,0,0"/>
    </Grid>
</Window>
